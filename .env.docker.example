# ShopBoard Docker Compose Configuration
# Copy this file as .env in the same directory as docker-compose.yml

# Host port to expose (default: 8000)
APP_PORT=8000

# Laravel environment
APP_ENV=production
APP_DEBUG=false

# Public URL
APP_URL=http://localhost:8000
